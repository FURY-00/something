# Core
numpy
requests
pyyaml
psutil

# Ears and voice
sounddevice
faster-whisper>=1.0
piper-tts>=1.3
pyttsx3
onnxruntime

# openWakeWord ("Hey Jarvis") is installed separately with --no-deps because it
# pins tflite-runtime, which doesn't exist for newer Pythons. These are the
# dependencies it actually needs:
scipy
scikit-learn
tqdm

# Hands
pyautogui
pyperclip
python-docx

# Engineering calculator
sympy
matplotlib

# Documents and websites
pypdf
python-pptx

# Professional apps
mcp>=1.10                      # Canva's official connector (and any other MCP connector)
mph                            # COMSOL (only used if COMSOL is installed)
pywin32; sys_platform == "win32"   # SolidWorks (Windows only)
# Ansys: pip install ansys-mapdl-core   (large; the installers add it when Ansys is found)

# Optional cloud brain (Claude). Only used if you set provider: anthropic
anthropic
